[project]
name = "rita2api"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "python-dotenv>=1.2.2",
    "requests>=2.31.0",
]

[project.scripts]
rita2api = "server:main"
